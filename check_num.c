#include <stdio.h>

// Input a number and check if it is greater than 50.

int main()
{
    int a;

    printf("Enter a Number: ");
    scanf("%d", &a);

    if (a > 50)
    {
        printf("A is greater than 50");
    }
    else
    {
        printf("A is less than or equal to 50");
    }

   
}
